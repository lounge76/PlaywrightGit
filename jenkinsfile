pipeline {
   agent { docker { image 'mcr.microsoft.com/playwright:v1.38.0-jammy' } }
   stages {
      stage('e2e-tests') {
         steps {
            bat  'ci'
            bat  'npx playwright test'
         }
      }
   }
}